import {Button} from "@components/button"

namespace Suite {
    function setup() {}
    function cleaup() {}

    class Tests {
        notebook_list_displayed(expects) {
            return expects.condition(true)
        }
    }
}