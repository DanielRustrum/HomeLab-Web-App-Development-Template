# Dev example (local docker db)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=homelab_app
DB_USER=app
DB_PASSWORD=app_password

# Or use a single URL:
# DATABASE_URL=postgresql://app:app_password@localhost:5432/homelab_app

APP_HOST=127.0.0.1
APP_PORT=8080
APP_ENV=dev
